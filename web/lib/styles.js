// Shared surface styles. Was duplicated verbatim across six files, which
// meant a theme fix had to be made six times.
export const cardClass =
  "bg-card text-card-foreground rounded-xl shadow-sm border border-border";

export const mutedText = { color: "hsl(var(--muted-foreground))" };
